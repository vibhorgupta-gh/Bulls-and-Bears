language : node_js
node_js :
    - "8.11.4"
services:
  - mongodb
before_script:
  - sleep 15  
install:
  - yarn install
script:
  - yarn test  
addons:
  apt:
    config:
      retries: true
    ⋮ # rest (including `packages` and `sources`, if applicable
   